name: 🗄 Database Migration

on:
  push:
    branches: [main]
    paths:
      - 'supabase/migrations/**'
  workflow_dispatch:

jobs:
  migrate:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Apply migrations via psql
        env:
          SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
        run: |
          # Installa psql
          sudo apt-get install -y postgresql-client

          # Connessione al DB Supabase (connection pooler)
          DB_URL="postgresql://postgres.nnsylkjahuhttwajuxls:${SUPABASE_DB_PASSWORD}@aws-0-eu-central-1.pooler.supabase.com:5432/postgres"

          # Applica ogni migration in ordine
          for f in $(ls supabase/migrations/*.sql | sort); do
            echo "▶ Applying: $f"
            psql "$DB_URL" -f "$f" -v ON_ERROR_STOP=1
            echo "✅ Done: $f"
          done

          echo "🎉 All migrations applied!"
